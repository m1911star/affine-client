{
  "name": "affine-client",
  "version": "0.1.0",
  "dependencies": {
    "@tauri-apps/api": "^1.0.2"
  },
  "scripts": {
    "tauri": "tauri",
    "dev:client": "yarn tauri dev",
    "build:client": "yarn tauri build",
    "dev:affine": "cd affine && pnpm start",
    "build:affine": "cd affine && pnpm build:local"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^1.0.5",
    "cross-env": "^7.0.3"
  }
}